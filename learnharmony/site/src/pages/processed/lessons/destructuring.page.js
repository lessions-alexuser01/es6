export var page = {
    "title": "Variable Destructuring",
    "navGroup": ".lessons",
    "next": "lessons/destructuring-arrays",
    "nextText": "Destructuring isn't just for objects.  You can Destructure Arrays too!",
    "heading": "Object Destructuring",
    "code": "let person = {\n    first: 'Marsellus',\n    last: 'Wallace',\n    spouse: {\n        first: 'Mia',\n        last: 'Wallace'\n    }\n};\n\nlet {first: husband, last, spouse: { first: wife } } = person;\nconsole.log(wife, husband, last);",
    "intro": "<p>Variable Destructuring is a syntactical convenience designed to help you destructure objects and arrays.  </p>\n<p>Let&#39;s say you have a person object:</p>\n<pre><code>var <span class=\"hljs-keyword\">person</span> = {\n    first: 'Marsellus',\n    last: 'Wallace'\n};\n</code></pre><p>You can easily assign variables to these properties using destructuring:</p>\n<pre><code>let {first, last} = <span class=\"hljs-keyword\">person</span>;\nconsole.log(first, last);\n</code></pre><p>You can also assign the variables using different names:</p>\n<pre><code>let {first: fn, last: ln} = <span class=\"hljs-keyword\">person</span>;\nconsole.log(fn, ln);\n</code></pre><p>This is particularly useful when you have a function that returns two values:</p>\n<pre><code>function getResult() {\n    <span class=\"hljs-keyword\">return</span> {\n        <span class=\"hljs-literal\">result</span>: <span class=\"hljs-number\">1234</span>,\n        error: null\n    };\n}\n\n<span class=\"hljs-keyword\">let</span> {<span class=\"hljs-literal\">result</span>, error} = getResult();\n</code></pre><p>But you can also use destructuring to pass variables in:</p>\n<pre><code>function <span class=\"hljs-keyword\">doHTTP({</span><span class=\"hljs-keyword\">host</span>, port, <span class=\"hljs-built_in\">path</span><span class=\"hljs-keyword\">}</span><span class=\"hljs-keyword\">) {</span>\n    console.log(<span class=\"hljs-keyword\">host</span>, port, <span class=\"hljs-built_in\">path</span>);\n<span class=\"hljs-keyword\">}</span>\n\n<span class=\"hljs-keyword\">doHTTP({</span>\n    <span class=\"hljs-keyword\">host</span>: <span class=\"hljs-string\">'learnharmony.org'</span>,\n    port: <span class=\"hljs-number\">80</span>,\n    <span class=\"hljs-built_in\">path</span>: <span class=\"hljs-string\">'/'</span>\n<span class=\"hljs-keyword\">}</span>);\n</code></pre><p>You can even destructure deeply:</p>\n<pre><code>let <span class=\"hljs-keyword\">person</span> = {\n    first: 'Marsellus',\n    last: 'Wallace',\n    spouse: {\n        first: 'Mia',\n        last: 'Wallace'\n    }\n};\n\nlet {first: husband, last, spouse: { first: wife } } = <span class=\"hljs-keyword\">person</span>;\nconsole.log(wife, husband, last);\n</code></pre>"
};